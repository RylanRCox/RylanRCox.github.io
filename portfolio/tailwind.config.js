const plugin = require('tailwindcss/plugin');
const fluid = require('tailwindcss-fluid');

module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}",
  ],
  theme: {
    extend: {},
  },
  plugins: [
  ],
}